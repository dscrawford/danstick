name: padmap
author: padmap
version: 0.1.0
summary: Game library with a press-to-activate controller setup screen
description: Pairs with the padmap daemon. Press Details (I, or X on a pad)
  from the library to open the controller order screen, hold a button on each
  controller in the order you want them assigned, then hold again on an
  assigned controller to confirm.
