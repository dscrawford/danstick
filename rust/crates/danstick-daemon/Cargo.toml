[package]
name = "danstick-daemon"
description = "The danstick daemon: the socket, the session, and the modal flows."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
danstick-core = { path = "../danstick-core" }
danstick-input = { path = "../danstick-input" }
evdev = "0.13"
log = "0.4"
rustix = { version = "1", features = ["event", "process", "std", "net"] }
serde_json = { workspace = true }
thiserror = "2"

[lints]
workspace = true
